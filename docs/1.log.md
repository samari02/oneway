# Changelog

> **Instructions** : Avant chaque commit, ajouter une entrée ici.  
> Format : `[#N] Description` avec lien vers la section détaillée.

---

## Index

1. [Initial setup — monorepo + shared types + Supabase schema](#1-initial-setup)
2. [Supabase config + Architecture doc](#2-supabase-config)
3. [Desktop app scaffold](#3-desktop-app-scaffold)
4. [Concept & Vision document](#4-concept-vision)
5. [Roadmap V1 détaillée](#5-roadmap-v1)
6. [Architecture rigoureuse](#6-architecture)
7. [Restructure + Habits feature + Clarity design](#7-restructure)
8. [Onboarding flow + Window size](#8-onboarding)
9. [Habits enrichis](#9-habits-enrichis)
10. [UI Polish + Optimistic updates](#10-ui-polish)
11. [Left sidebar navigation](#11-left-sidebar)
12. [UI fixes — icons, fixed layout, window size](#12-ui-fixes)
13. [UX improvements — click to toggle, edit/delete, sidebar pin](#13-ux-improvements)
20. [Timeline view with scheduled times + glow effect](#20-timeline)
21. [Fix state sync issue when switching tabs](#21-state-sync)
22. [Feature ideas doc + Stats basiques](#22-stats)
23. [Hero Mascot + Animations polish](#23-mascot-hero)
24. [Mascot legs + Hi Name greeting](#24-mascot-legs)
25. [Reset onboarding dev button](#25-reset-onboarding)
26. [Transparent titlebar + Profile icon](#26-titlebar)
27. [System tray icon + Cute profile button](#27-tray-icon)
28. [Product principles + Habits/Boundaries unified system + Welcome onboarding](#28-principles-boundaries)
29. [UI improvements — Stats fix, Now marker, Emoji picker, Time slider](#29-ui-improvements)
30. [Habits & Boundaries implementation + AddHabitModal](#30-habits-boundaries-impl)
31. [Mascot kawaii redesign](#31-mascot-kawaii)
32. [North Star goal feature](#32-north-star)
33. [AI goal refinement with OpenAI](#33-ai-goal-refinement)
34. [Empathetic AI coach with context & memory](#34-ai-empathetic)
35. [AI Companion integrated in header](#35-ai-companion)
36. [AI Companion UI polish — pills, typing, chat-first](#36-ai-companion-polish)
37. [Mascot as chat trigger, scroll fix, text visibility](#37-ux-polish)
38. [Chat button at feet, full-width drawer, smaller nav](#38-chat-ux)
39. [Hover on Aoi to chat, inline in header](#39-aoi-chat)
40. [Visual timeline view with toggle](#40-visual-timeline)
41. [Goals layer with pills UI](#41-goals-layer)
42. [AI Chat History](#42-ai-chat-history)
43. [Mascot Polish + UI Refinements](#43-mascot-polish)
44. [Today's Focus Progress Bar](#44-todays-focus)
45. [Sticky Hero Header](#45-sticky-hero)
46. [Dark Mode Support](#46-dark-mode)
47. [Stats Page Improvements](#47-stats-improvements)
48. [Browser Extension MVP](#48-browser-extension)
49. [Extension Bypass Fix](#49-extension-bypass-fix)
50. [Extension Best Practices Documentation](#50-extension-best-practices)
51. [Rebrand Oneway to Clarity](#51-rebrand-clarity)
52. [History Collection Implementation](#52-history-collection)
53. [Supabase Sync for Navigation History](#53-supabase-sync)
54. [Native Messaging Implementation](#54-native-messaging)
55. [Browsing Insights Dashboard](#55-browsing-insights)
56. [Complete Data Pipeline](#56-data-pipeline)
57. [History Import with Period Selection](#57-history-import)
58. [Data Deduplication & Extension Reset](#58-data-deduplication)
59. [Stats UI Polish & Period Selector](#59-stats-ui-polish)
60. [Period Filtering Implementation](#60-period-filtering)
61. [Per-Card Period Override & Window Size](#61-per-card-override)
62. [Per-Card Data Fetching & UI Polish](#62-per-card-fetching)
63. [Enhanced Period Menu UI & Complete Coverage](#63-period-menu-complete)
64. [Stats Cards Final Polish](#64-stats-cards-polish)
65. [Global Period Reset & Text Visibility](#65-global-reset-visibility)
66. [Site Classification Modal](#66-site-classification)
67. [Habits Calendar View + Timezone Fix + Inline Reclassify](#67-habits-calendar)
68. [Calendar Drag & Drop with 15-min Snap](#68-calendar-drag-snap)
69. [Modal UI Polish + Delete from Edit](#69-modal-polish)
70. [Blocking System: SafeSearch + Search Filter + Protection Status](#70-blocking-system)
71. [SafeSearch Expansion — All Major Search Engines](#71-safesearch-expansion)
72. [Heartbeat System & Protection Alerts (Phase 2)](#72-heartbeat-system)
73. [Aoi Floating Widget — Browser Companion](#73-aoi-widget)
74. [Aoi Widget Options Menu — Hide Global & Per Domain](#74-aoi-options-menu)
75. [Aoi Preferences Persistence — Supabase Sync](#75-aoi-preferences-sync)
76. [Intelligent Blocking — Phase 1 & 2 Implementation](#76-intelligent-blocking)
77. [Anti-Evasion Improvements — Repeated Characters & Japanese](#77-anti-evasion-improvements)

---

## Entries

### #1 Initial setup

**Date:** 2026-01-09

- Monorepo pnpm workspace créé
- Structure : `apps/desktop`, `apps/extension`, `packages/shared`
- `@oneway/shared` avec types de base :
  - `Habit`, `HabitCheckIn`, `HabitStreak`
  - `BlockingRule`, `BlockingState`, `BlockingMode`
  - `User`, `UserSettings`
- Migration SQL Supabase (`001_initial_schema.sql`)
  - Tables : `habits`, `habit_check_ins`, `blocking_rules`, `blocking_state`, `user_settings`
  - RLS activé sur toutes les tables
- Dossier `docs/` créé pour la documentation

---

### #2 Supabase config

**Date:** 2026-01-09

- Anon key ajoutée à `constants.ts`
- Tables Supabase créées et vérifiées
- `docs/architecture.md` — principes architecturaux et best practices
  - Explication Tauri vs Electron
  - Communication patterns (App ↔ Extension ↔ Supabase)
  - Conventions TypeScript/React/Git

---

### #3 Desktop app scaffold

**Date:** 2026-01-09

- Tauri + React app dans `apps/desktop`
- Connexion Supabase configurée
- Auth magic link fonctionnel
- UI de base :
  - Login screen
  - Dashboard avec habits (placeholder)
  - Focus mode status
  - Dark theme

---

### #4 Concept & Vision

**Date:** 2026-01-09

- `docs/concept.md` — Document complet de vision produit
- Rebrand : oneway → **Clarity**
- Onboarding flow défini (problèmes → best self → strictness)
- UX quotidienne (timeline, swipe, nudges)
- Blocking intelligent basé sur objectifs
- 3 modes : Gentle / Guided / Strict
- Design direction : Headspace + Ghibli vibes
- Palette : Mint/turquoise, pastels doux
- Mascotte : Petit blob inspiré Susuwatari
- Tone : Ami direct, pas de bullshit
- Features priorisées V1/V2/V3

---

### #5 Roadmap V1 détaillée

**Date:** 2026-01-09

- Refonte complète de `docs/roadmap.md`
- 5 phases définies : Foundations → Habits → Extension → Blocking → Polish
- Chaque phase décomposée en sous-tâches
- Backlog V2+ séparé
- Current focus : Phase 1.2 — UI Habits

---

### #6 Architecture rigoureuse

**Date:** 2026-01-09

- Refonte complète `docs/architecture.md`
- Feature-based folder structure définie
- Data layer patterns (API + hooks)
- State management strategy (local first, Context si partagé)
- Component patterns (Container vs Presentational)
- Naming conventions (files, code, CSS)
- Error handling patterns
- Extension architecture
- Communication patterns (App ↔ Supabase ↔ Extension)
- Testing strategy
- Git workflow + checklist PR

---

### #7 Restructure + Habits feature + Clarity design

**Date:** 2026-01-09

- Restructuration complète selon architecture.md
- Feature-based folders : `features/auth`, `features/habits`
- Auth feature :
  - `AuthContext` + `useAuth` hook
  - `LoginForm` component
- Habits feature :
  - API layer (`api/habits.ts`)
  - Hooks : `useHabits`, `useTodayCheckIns`, `useHabitActions`
  - Components : `HabitList`, `HabitItem`, `AddHabitForm`
- Nouveau design Clarity :
  - CSS variables (`variables.css`)
  - Global styles (`global.css`)
  - Palette mint/pastels
  - Typo Nunito
  - Mascotte 💧
- Path aliases configurés (`@/`, `@oneway/shared`)

---

### #8 Onboarding flow + Window size

**Date:** 2026-01-09

- Fenêtre agrandie (480x720) + renommée "Clarity"
- Feature onboarding complète :
  - Step 1 : Problèmes (sélection multiple)
  - Step 2 : Best Self (heures réveil/coucher/écrans)
  - Step 3 : Strictness (Gentle/Guided/Strict)
  - Step 4 : Résumé + setup auto
- Habits créés automatiquement selon problèmes sélectionnés
- Migration SQL pour user_settings (wake_time, sleep_time, etc.)
- Mascotte change quand tous habits complétés ✨

---

### #9 Habits enrichis

**Date:** 2026-01-09

- Migration SQL : nouveaux champs habits (description, duration_minutes, is_required, time_of_day)
- Types partagés mis à jour (TimeOfDay, champs enrichis)
- AddHabitForm enrichi :
  - Sélection time of day (Matin/Soir/Anytime)
  - Durée avec presets (5, 10, 15, 20, 30, 45, 60 min)
  - Description optionnelle
  - Toggle "Required for unblock"
  - Section "More options" repliable
- HabitItem amélioré :
  - Badge "Required" pour habits bloquants
  - Affichage durée et description
  - Zone cliquable pour édition future

---

### #10 UI Polish + Optimistic updates

**Date:** 2026-01-09

- **Optimistic updates** : check/uncheck instantané (UI se met à jour immédiatement, sync en background)
- **Animations** :
  - Check "pop" animation
  - Progress bar avec easing bouncy
  - Celebration animation quand tout est complété
  - Mascotte hover effect
- **Visual polish** :
  - Shadows sur les habit items
  - Hover lift effect
  - Card pour la section "Today"
  - Gradient sur le titre "Clarity"
  - Progress bar dans une card
  - Meilleure hiérarchie visuelle

---

### #11 Left sidebar navigation

**Date:** 2026-01-09

- **Sidebar collapsible** : expand au hover (64px → 160px)
- **3 vues** : Today, Stats, Settings
- **Stats view** : placeholder avec preview des cards (streaks, weekly, best)
- **Settings view** : section account (logout), placeholders pour strictness/blocked sites/schedule
- **Layout refactorisé** : App avec sidebar + content area
- **Fenêtre élargie** : 480px → 580px pour accommoder la sidebar

---

### #12 UI fixes — icons, fixed layout, window size

**Date:** 2026-01-09

- **Icônes SVG** : Remplacé emojis par SVG monochrome (calendar, bar-chart, settings)
- **Sidebar fixe** : `position: fixed` — ne scroll plus avec le contenu
- **Header sticky** : Reste visible au scroll
- **Sidebar plus compacte** : 52px (vs 64px)
- **Fenêtre plus grande** : 720×800 (vs 580×720)

---

### #13 UX improvements — click to toggle, edit/delete, sidebar pin, larger window

**Date:** 2026-01-09

- **Fenêtre encore plus grande** : 960×800 (vs 720×800)
- **Police habits réduite** : font-size-sm, icons 14px, checkbox 22px
- **Sidebar pin/unpin** : bouton en bas pour garder sidebar ouverte ou auto-collapse
- **Click anywhere to toggle** : cliquer sur tout l'item toggle le check (pas juste le bouton)
- **Menu actions** : bouton ⋮ sur hover pour Edit/Delete
- **Edit modal** : modification de tous les champs (name, icon, description, duration, required, time)
- **Delete** : confirmation avant suppression

---

### #20 Timeline view with scheduled times + glow effect

**Date:** 2026-01-09

- **Migration SQL** : Ajout `scheduled_time` (HH:MM) à la table habits
- **Timeline verticale** :
  - Ligne continue reliant les habits
  - Points colorés (vert = done, glow = current)
  - Heure affichée à gauche de chaque habit
  - Habits triés par `scheduled_time`
- **Glow effect** : Pulse subtil (3s) sur l'habit en cours (premier non-coché)
- **AddHabitForm** : Champ time picker pour l'heure prévue
- **EditHabitModal** : Champ time picker pour modifier l'heure

---

### #22 Feature ideas doc + Stats basiques

**Date:** 2026-01-10

- **`docs/feature-ideas.md`** : Document complet des idées de features futures
  - Habits & Tasks enrichis
  - Visualisation du temps (day/week/month views)
  - Stats & Analytics (basiques + avancées + gamification)
  - Blocking & Strictness
  - Mascotte & Animations (Rive)
  - Notifications & Nudges
  - Multi-platform plans
- **Stats feature complète** :
  - API layer (`api/stats.ts`) pour fetch check-ins
  - Utilitaires calcul (`utils/calculations.ts`) :
    - `calculateCurrentStreak` (basé sur required habits)
    - `calculateBestStreak` (record historique)
    - `calculateCompletionRate` (7j / 30j)
    - `calculateHabitStats` (par habit individuel)
  - Hook `useStats` — data fetching + calculs
  - Components :
    - `StreakCard` : Fire emoji animé, dots, best streak
    - `CompletionBar` : Barre de progression semaine/mois
    - `HabitStatsCard` : Stats par habit (14 jours)
    - `MascotMessage` : Blob animé + message encourageant
  - `StatsView` : Vue complète avec animations staggered

---

### #23 Hero Mascot + Animations polish

**Date:** 2026-01-10

- **Mascot composant partagé** (`features/mascot`)
  - 5 moods : happy, proud, encouraging, sleepy, thinking
  - 3 tailles : small, medium, large
  - Speech bubble avec message
  - Animations CSS fluides (float, pulse)
- **Hero mascot dans Today view** : Mascot proéminent avec greeting
- **Couleur cohérente** : Base violet/lavender, seul le glow change selon le mood
- **Transitions fluides** : Animation unique `blobFloat` pour tous les moods

---

### #24 Mascot legs + Hi Name greeting

**Date:** 2026-01-10

- **Petites jambes** : CSS pseudo-elements (::before/::after) avec wiggle animation
- **Greeting personnalisé** : "Hi {Name}!" basé sur `display_name`
- **Migration SQL** : `005_user_display_name.sql` — ajout colonne `display_name`
- **Onboarding mis à jour** : Demande le prénom dans StepProblems
- **useUserSettings hook** : Fetch et expose les settings utilisateur
- **Mascot plus clair** : `filter: brightness(1.05)` par défaut

---

### #25 Reset onboarding dev button

**Date:** 2026-01-10

- **Dev Tools section** dans Settings
- **Bouton "Reset Onboarding"** : Supprime user_settings et habits, recharge l'app
- **Style discret** : Bordure dashed, opacity réduite, section en bas

---

### #26 Transparent titlebar + Profile icon

**Date:** 2026-01-10

- **Titlebar macOS overlay** : `titleBarStyle: "Overlay"` + `hiddenTitle: true`
- **Barre de titre custom** : 38px de haut, fixed en haut
  - Traffic lights intégrés (70px spacer)
  - "Clarity" centré avec gradient
  - Icône profil (SVG user) à droite → navigue vers Settings
- **Layout restructuré** :
  - `.app-titlebar` en position fixed
  - `.app-layout__body` contient sidebar + content
  - Sidebar démarre sous la titlebar
- **Drag region** : `data-tauri-drag-region` pour déplacer la fenêtre

---

### #27 System tray icon + Cute profile button

**Date:** 2026-01-10

- **System tray (menu bar icon)** :
  - Feature `tray-icon` activée dans Cargo.toml
  - Icône dans la barre de menu macOS
  - Clic gauche : toggle show/hide de la fenêtre
  - Clic droit : menu contextuel (Show, Hide, Quit)
  - Code Rust dans `lib.rs` avec TrayIconBuilder
- **Icône profil améliorée** :
  - Cercle avec gradient violet (mascot colors)
  - Ombre douce et effet hover scale
  - Navigue vers Settings au clic

---

### #28 Product principles + Habits/Boundaries unified system + Welcome onboarding

**Date:** 2026-01-10

- **Product Principles** (`docs/product-principles.md`) :
  - 3 piliers fondamentaux : Simplicité, Plaisir, Guide
  - Guidelines pour chaque décision produit
  - Tone of voice défini
  - Ce qu'on ne fait JAMAIS (culpabilité, stats négatives, etc.)

- **Habits & Boundaries unified** (`docs/habits-boundaries.md`) :
  - Distinction DO (faire) vs AVOID (éviter)
  - Boundaries = habits de type "avoid"
  - 2 catégories : Digital (auto via blocking) / Physical (honor system)
  - Approche optimiste : auto-validé, intervenir seulement si fail
  - Data model défini (SQL + TypeScript)
  - Plan d'implémentation en 5 phases

- **Onboarding Welcome screen** :
  - Nouvelle étape 1 : StepWelcome
  - Storytelling sur la vie digitale inconsciente
  - "On scroll, on refresh, on dérive..."
  - Présentation de Clarity comme guide, pas surveillant
  - 3 principes en footer compact

- **UI fixes** :
  - Icône profil : gradient violet + icône blanche pleine
  - Window drag : API Tauri `startDragging()` + permission
  - Comportement fermeture simplifié (plus de tray, quit normal)

---

### #29 UI improvements — Stats fix, Now marker, Emoji picker, Time slider

**Date:** 2026-01-10

- **Stats progress bar fix** :
  - Corrigé variables CSS manquantes (`--accent` → `--color-mint`)
  - La barre de progression reflète maintenant le vrai pourcentage

- **Now marker inline** :
  - Marqueur "now" positionné sur la ligne de connexion timeline
  - Point corail pulsant + heure actuelle
  - Plus d'espace supplémentaire entre les habits
  - Se met à jour toutes les minutes

- **Emoji picker** (`emoji-picker-react`) :
  - Nouveau composant `EmojiPicker.tsx`
  - Popup avec recherche et catégories
  - Intégré dans AddHabitForm et EditHabitModal
  - Style customisé pour matcher Clarity

- **Time slider cute** :
  - Nouveau composant `TimePicker.tsx`
  - Slider 00:00 → 23:59 avec fond dynamique :
    - Bleu ciel (jour), Rose (dawn/dusk), Bleu nuit (nuit)
  - Soleil ☀️ / Lune 🌙 animés qui suivent le slider
  - Boutons +/- pour ajuster par 5 minutes
  - Remplace le time input natif peu user-friendly

- **Add Habit button** :
  - Redesign moderne : ghost button sans bordure dashed
  - Plus subtil, opacity réduite, hover mint

---

### #30 Habits & Boundaries implementation + AddHabitModal

**Date:** 2026-01-10

- **DB Migrations** :
  - `006_habits_boundaries.sql` : Ajout `habit_type` (do/avoid), `avoid_category` (digital/physical), `time_start`, `time_end`, `blocked_sites`, `days_of_week`
  - `007_checkins_boundaries.sql` : Ajout `completed`, `violation_count`, `bypass_timestamps` pour le tracking

- **Types partagés** :
  - Nouveaux types : `HabitType`, `AvoidCategory`
  - Interface `Habit` enrichie avec tous les champs boundary
  - Interface `HabitCheckIn` avec tracking violations

- **AddHabitModal** (remplace AddHabitForm inline) :
  - Modal popup comme EditHabitModal
  - Toggle Do/Avoid avec couleurs brand (mint/lavender)
  - Champs conditionnels si Avoid :
    - Catégorie (Digital 📱 / Physical 🍎)
    - Période active (time range)
    - Jours actifs (M T W T F S S)
    - Sites à bloquer (si digital)
  - Proportions cohérentes (520px, champs 42px, borders 1px)
  - TimePicker compact sans icône devant l'heure

- **HabitItem boundaries** :
  - Bordure gauche lavender pour les boundaries
  - Indicateur de statut : ⏳ (actif), ✓ (respecté), ✗ (violé)
  - Affichage time range au lieu de scheduled_time
  - Action "I didn't hold" dans le menu

- **EditHabitModal aligné** :
  - Mêmes proportions que AddHabitModal
  - Labels uppercase, champs uniformes
  - TimePicker compact identique

---

### #31 Mascot kawaii redesign

**Date:** 2026-01-10

- **Style kawaii** :
  - Petits yeux ronds (dots SVG) au lieu de texte/emojis
  - Différentes expressions selon le mood (happy, proud, encouraging, sleepy, thinking)
  - Petites joues roses (blush) positionnées sur les côtés
  - Blob parfaitement rond avec gradient lavender/mint

- **Jambes redesignées** :
  - Petites jambes en forme de haricot
  - Animation wiggle subtile
  - Positionnées en dessous du blob (z-index -1)

- **Code refactorisé** :
  - SVG pour les yeux/bouche dans `FACES_SVG` map
  - Composant `MascotCheeks` pour les joues
  - Composant `MascotLegs` pour les jambes
  - CSS animations maintenues (float, wiggle)

---

### #32 North Star goal feature

**Date:** 2026-01-10

- **Concept** (`docs/north-star.md`) :
  - Objectif principal à long terme pour guider les habitudes quotidiennes
  - Toujours visible dans le dashboard
  - Possibilité de lier les habits au North Star

- **DB Migration** (`008_north_star.sql`) :
  - `user_settings` : `north_star_goal`, `north_star_icon`, `north_star_created_at`
  - `habits` : `linked_to_north_star` boolean

- **Onboarding** (`StepNorthStar.tsx`) :
  - Nouvelle étape après les problèmes (step 3)
  - Input texte avec sélecteur d'icône
  - "What's your North Star?" avec exemples

- **Today view display** :
  - Badge compact à droite de la date
  - Icône + texte tronqué + edit icon au hover
  - Style doré/ambre pour le distinguer

- **Edit modal** (`NorthStarEditModal.tsx`) :
  - Modification du goal et de l'icône
  - Liste des habits "Do" avec checkboxes
  - Permet de lier/délier les habits au North Star
  - Sauvegarde en batch (settings + habits)

---

### #33 AI goal refinement with OpenAI

**Date:** 2026-01-10

- **OpenAI Service** (`lib/openai.ts`) :
  - Intégration GPT-4o-mini pour coût minimal (~$0.01/conversation)
  - Clé API stockée localement (localStorage)
  - System prompt optimisé pour le coaching de goals
  - Parsing JSON des suggestions structurées

- **Settings** :
  - Nouvelle section "✨ AI Features"
  - Champ pour entrer/modifier la clé API
  - Boutons Save/Remove avec feedback visuel
  - Input masqué (password) avec toggle visibility

- **AI Chat dans North Star modal** :
  - Bouton "✨ Refine with AI" (visible si clé configurée)
  - Interface chat conversationnelle
  - Messages user/assistant stylisés
  - Indicateur de chargement animé

- **Suggestions structurées** :
  - Goal raffiné affiché dans un encart vert
  - Liste d'habits suggérés avec :
    - Nom, icône, horaire suggéré
    - Checkboxes pour sélectionner lesquels garder
  - Bouton "Apply suggestions" pour adopter le goal

- **UX** :
  - Modal plus large (520px) et plus haute (min 400px)
  - Zone de chat scrollable (280px max)
  - Transitions fluides et feedback visuel

---

### #34 Empathetic AI coach with context & memory

**Date:** 2026-01-10

- **Prompt empathique réécrit** :
  - Personnalité chaleureuse, patiente, genuinement curieuse
  - Valide les émotions avant de conseiller
  - Pose des questions profondes sur le "pourquoi"
  - Utilise le prénom de l'utilisateur
  - Répond dans la langue de l'utilisateur

- **Approche structurée** :
  - D'abord comprendre la motivation profonde
  - Identifier les obstacles actuels
  - Visualiser le succès
  - Seulement après → suggérer des actions

- **Contexte utilisateur passé à l'AI** :
  - `displayName` — prénom pour personnalisation
  - `currentGoal` — objectif actuel
  - `habits` — liste des habitudes existantes
  - `wakeTime` / `sleepTime` — horaires de vie
  - `previousConversations` — résumé des échanges passés

- **Mémoire persistante** (`009_ai_conversations.sql`) :
  - Nouvelle table `ai_conversations` dans Supabase
  - Stocke messages + contexte par utilisateur
  - Fonctions `saveConversation`, `loadConversation`, `clearConversation`
  - `getPreviousConversationsSummary` pour inclure l'historique
  - RLS pour sécurité des données

- **Intégration modal** :
  - `NorthStarEditModal` reçoit `userSettings` en props
  - Contexte construit dynamiquement à chaque message
  - Conversations sauvegardées automatiquement

---

### #35 AI Companion integrated in header

**Date:** 2026-01-10

- **Nouveau composant `AICompanion`** (`features/ai-companion/`) :
  - Bouton "✨ Parle-moi" discret dans le header
  - Click → le header expand vers le bas
  - Panel de conversation intégré

- **Modes de conversation** :
  - 🎯 **Ma North Star** — Travailler sur l'objectif principal
  - 🔧 **Mes habitudes** — Créer, ajuster, optimiser
  - 📊 **Mon progrès** — Analyser les check-ins et patterns
  - 📝 **Mes tâches** — Placeholder (coming soon)

- **Greetings contextuels** :
  - "Comment je peux t'aider ?"
  - "De quoi tu veux parler aujourd'hui ?"
  - Variations selon l'heure (matin/soir/nuit)
  - Utilise le prénom de l'utilisateur

- **UX** :
  - Sélection de mode via boutons icônes (2x2 grid)
  - Interface chat complète après sélection
  - Bouton retour pour changer de mode
  - Animation d'expansion fluide
  - Responsive design

- **Product Principles mis à jour** :
  - Nouvelle section "Mission centrale"
  - Citation clé : "Clarity connecte ta vie digitale et ta vie personnelle pour les aligner vers ce qui compte vraiment pour toi."
  - Explication du conflit vie réelle / vie digitale

---

### #36 AI Companion UI polish — pills, typing, chat-first

**Date:** 2026-01-10

- **Pills élégantes** :
  - Remplacement des gros boutons par des pills compactes
  - Layout inline (flex-wrap)
  - Hover avec bordure mint
  - "Tâches" grisé/disabled

- **Chat-first design** :
  - Input toujours visible, pas besoin de sélectionner un mode
  - Placeholder contextuel
  - Auto-focus sur l'input à l'ouverture
  - Écriture directe possible sans mode

- **Typing animation** :
  - Messages AI lettre par lettre (15ms/char)
  - Curseur `|` clignotant pendant la frappe
  - Hook `useTypingAnimation` réutilisable
  - Composant `TypingMessage` dédié

- **Documentation** :
  - `docs/ai-companion.md` créé
  - Architecture, modes, prompt engineering
  - Next steps détaillés (Phases 1-5)

---

### #37 Mascot as chat trigger, scroll fix, text visibility

**Date:** 2026-01-10

- **Mascot clickable** :
  - Le message de la mascotte ouvre maintenant le chat AI
  - Icône ✨ en hint au hover
  - Bouton "Parle-moi" supprimé (mascotte prend le relais)
  - Props `onMessageClick`, `isOpen`, `onOpenChange` pour contrôle externe

- **Scroll fix** :
  - Le chat scroll maintenant uniquement dans sa zone de messages
  - `scrollIntoView` remplacé par `scrollTop` sur le container
  - La page entière ne bouge plus quand on tape

- **Visibilité texte améliorée** :
  - Nouveau `--text-nav: #6B7A8F` pour nav et heures
  - `--text-muted` légèrement plus foncé (#8B9DB5)
  - Items nav sélectionnés en `semibold`
  - Meilleur contraste global

---

### #38 Chat button at feet, full-width drawer, smaller nav

**Date:** 2026-01-10

- **Bouton chat aux pieds** :
  - Nouveau bouton 💬 sous la mascotte
  - Toujours visible, clairement interactif
  - La bulle reste un simple greeting

- **Drawer full-width** :
  - Chat panel en dessous du header
  - Prend toute la largeur
  - Slide down animation
  - Ne déplace plus la mascotte

- **Nav plus compacte** :
  - Font réduite : 14px → 12px (items)
  - Pin button : 12px → 11px
  - Look plus clean et moins encombrant

---

### #39 Hover on Aoi to chat, inline in header

**Date:** 2026-01-10

- **Aoi interactive** :
  - Hover sur la mascotte → effet glow mint
  - Click ouvre/ferme le chat (toggle)
  - Bouton 💬 supprimé
  - La mascotte entière est cliquable

- **Chat inline dans header** :
  - Panel intégré dans le header (pas séparé)
  - Background mint léger quand chat ouvert
  - Animation smooth fade-in (cubic-bezier)

- **Cleanup** :
  - Icônes 📱/🍎 supprimées des cartes habit
  - Nav font ajustée : 12px → 13px

---

### #40 Visual timeline view with toggle

**Date:** 2026-01-10

- **Toggle de vue** :
  - ☰ = Vue liste (originale)
  - ▤ = Vue timeline visuelle (nouvelle)

- **Vue visuelle** :
  - Bannière "Active" pour boundaries en cours
  - Résumé boundaries avec barres hachurées coral
  - Blocs habits avec bordure mint, durée visible
  - Marqueur "Now" avec point coral

- **Distinction visuelle** :
  - Habits (DO) : blocs solides mint
  - Boundaries (AVOID) : barres hachurées coral

---

### #41 Goals layer with pills UI

**Date:** 2026-01-10

- **Goals table** (`010_goals.sql`) :
  - `id`, `user_id`, `name`, `icon`, `progress` (0-100), `target_date`
  - `goal_id` ajouté à la table `habits` pour liaison
  - RLS policies configurées

- **Types partagés** :
  - Interface `Goal` avec tous les champs
  - `Habit.goal_id` optionnel pour liaison

- **Goals feature** (`features/goals/`) :
  - API CRUD (`api/goals.ts`)
  - Hook `useGoals` (fetch, create, update, delete)
  - `GoalsBar` : pills compactes dans le hero
  - `GoalModal` : création/édition/suppression

- **UI Pills élégantes** :
  - Icône minimaliste (●, ◆, ★, ▲, ■, etc.)
  - Pourcentage de progression
  - Mini barre de progression dans la pill
  - Bouton + pour ajouter un goal
  - Click sur pill → modal d'édition

- **Intégration header** :
  - GoalsBar dans le hero, sous la North Star
  - Progression manuelle via slider
  - Target date optionnelle

---

### #42 AI Chat History

**Date:** 2026-01-11

- **Migration** (`011_ai_conversations_history.sql`) :
  - Suppression contrainte UNIQUE user_id
  - Ajout colonnes `title`, `mode`, `is_active`
  - Index pour lookup rapide

- **API history** (`lib/openai.ts`) :
  - `createConversation`, `updateConversation`
  - `listConversations` avec limite et preview
  - `getConversation` par ID
  - `deleteConversation`
  - Génération automatique de titre

- **UI Historique** :
  - Bouton 📜 (icône horloge) dans header chat
  - Panel avec liste des conversations passées
  - Date relative (Hier, Il y a 3 jours...)
  - Aperçu du dernier message
  - Actions: reprendre, supprimer, nouvelle conversation

---

### #43 Mascot Polish + UI Refinements

**Date:** 2026-01-11

- **Mascot CSS** :
  - Retour CSS pur (Rive testé mais abandonné)
  - Pousse verte sur la tête 🌱
  - Couleurs opaques (plus de transparence)
  - Plus de glow/ombre externe
  - Animation bounce réduite (3s, 2px)
  - Bouche cachée si yeux courbés (happy/proud/sleepy)
  - Bulle "Chat with Aoi" au hover

- **Hero** :
  - Gradient horizontal: blanc gauche → mint droite
  - Bordure plus visible
  - Messages sans emojis

- **Global** :
  - Focus/halo supprimé sur cartes et boutons
  - Labels GOALS/NORTH STAR plus foncés

---

### #44 Today's Focus Progress Bar

**Date:** 2026-01-11

- Section "Today's Focus" avec barre de progression
- Compteur X/Y visible
- Barre 200px avec gradient mint
- Positionnée sous le titre "Today's Focus"

---

### #45 Sticky Hero Header

**Date:** 2026-01-11

- Hero (mascotte + date + North Star + Goals) reste fixe en haut
- Le contenu (Today's Focus + habits) scroll en dessous
- Ombre subtile apparaît quand scrolled
- Chaîne flex complète avec `min-height: 0` pour scroll imbriqué
- Effet de minimisation testé mais abandonné (causait des saccades)

---

### #46 Dark Mode Support

**Date:** 2026-01-11

- **Toggle** : Bouton lune/soleil dans titlebar
- **Variables CSS** dark mode :
  - Backgrounds: navy/charcoal (#0F1419, #1A1F26, #242B33)
  - Text: softer whites (#E7E9EA, #8B98A5)
  - Borders: #2F3841, #3D4752
  - Mint reste vibrant mais légèrement ajusté
  - Shadows plus prononcés

- **Composants mis à jour** :
  - Mascot bubble
  - Goals pills (GoalsBar)
  - GoalModal
  - AICompanion panel + messages + input + history
  - Sidebar (meilleure visibilité texte)
  - TimePicker slider thumb

- **Persistence** : localStorage (`clarity-theme`)

---

### #47 Stats Page Improvements

**Date:** 2026-01-11

- **Full-width stats cards** :
  - Removed max-width: 900px constraint
  - Cards now extend across full window width
  
- **Dark mode fixed** :
  - Used CSS variables (--bg-elevated, --border-light) instead of hardcoded colors
  - Works properly in both light and dark themes
  
- **30-day Activity Heatmap** :
  - New HeatmapCard component with GitHub-style grid
  - 30 squares showing daily completion rates
  - 3 color levels: low (0-33%), medium (34-66%), high (67-100%)
  - Hover tooltips with date and completion details
  - Average completion percentage displayed
  - Legend "Less → More"
  - Positioned between StreakCard and CompletionBars
  
- **calculateDailyStats utility** :
  - Computes completion rate per day over 30 days
  - Handles future days (shown as dashed outlines)
  - Integrated in useStats hook

---

### #48 Browser Extension MVP

**Date:** 2026-01-11

- **Extension structure** (`apps/extension/`) :
  - Manifest V3 Chrome extension
  - TypeScript + Vite build system
  - Feature-based architecture ready for scaling
  
- **Service Worker** (`background/service-worker.ts`) :
  - Monitors navigation with webNavigation API
  - Checks URLs against blocklist
  - Redirects to block screen when matched
  - Logs block events to local storage
  
- **Hardcoded blocklist** :
  - Twitter/X, Facebook, Instagram, TikTok, Reddit
  - YouTube, Netflix (with 'ask' action)
  - News sites (NYTimes)
  - Pattern matching with wildcards
  
- **Block Screen UI** :
  - Beautiful full-page overlay when site is blocked
  - Shows mode (Focus Mode), domain, reason
  - 3 bypass options: Work-related, Quick break (5min), End mode
  - Radio buttons selection
  - Cancel (go back) / Continue buttons
  - Stats display (sites blocked today)
  
- **Extension Popup** :
  - Status display (mode, active/paused)
  - Block count today
  - Toggle mode button
  - Settings button (placeholder)
  
- **Shared utilities** :
  - Types (BlockRule, NavigationEvent, BlockEvent, etc.)
  - Constants (DEFAULT_BLOCKLIST, STORAGE_KEYS)
  - Utils (extractDomain, matchesPattern, logging)
  
- **Documentation** :
  - `docs/browser-extension.md` — Complete architecture doc
  - `apps/extension/README.md` — Dev setup guide
  - Phase 0 complete, ready for Phase 1 (history collection)
  
- **Next steps** :
  - Fix bypass flow (Continue button)
  - Add more test sites
  - History collection (30 days import)
  - Native Messaging to Electron app
  - Modes implementation
  - LLM integration

---

### #49 Extension Bypass Fix

**Date:** 2026-01-11

**Issue**: Bypass button was not working — clicking "Continue" would redirect to the original URL but immediately trigger a new blocking check, creating an infinite loop.

**Root cause**: The blocking logic had no memory of bypass decisions. Every navigation was checked against the rules, even after a user explicitly chose to bypass.

**Solution**: Implemented a **tab-specific allowlist** system:
- `allowedTabs: Map<tabId, { domain, expiresAt }>`
- When user bypasses, add tab+domain to allowlist with 5-minute expiration
- `shouldBlock()` checks allowlist BEFORE checking rules
- Automatic cleanup of expired entries every 5 minutes

**Changes**:
- Modified `service-worker.ts`:
  - Added `allowedTabs` map
  - Updated `shouldBlock()` to check allowlist first
  - Modified `handleBypass()` to add to allowlist
  - Added `cleanupAllowedTabs()` periodic task
- Modified `block-screen.ts`:
  - Send `tabId` with bypass message
  - Navigate to original URL after successful bypass

**Result**: Bypass now works correctly — user can access blocked site for 5 minutes in that specific tab.

---

### #50 Extension Best Practices Documentation

**Date:** 2026-01-11

Added comprehensive **Extension Best Practices** section to `docs/architecture.md`:

**Topics covered**:
1. **Service Worker Patterns**
   - Ephemeral nature (no memory state)
   - Tab-specific state management
   - Decision flow (allowlist → rules)

2. **Message Passing**
   - Type-safe message interfaces
   - Error handling (chrome.runtime.lastError)
   - Async response patterns

3. **Storage Patterns**
   - Structured storage interface
   - Typed helpers
   - Periodic cleanup automation

4. **Security Best Practices**
   - Content Security Policy
   - Minimal permissions
   - Input validation & sanitization
   - URL validation (no javascript:, data:, etc.)
   - HTML escaping
   - Privacy (no sensitive data logging)
   - Storage encryption (if needed)

5. **Testing Patterns**
   - Unit tests for logic
   - Mock Chrome APIs
   - Vitest setup

6. **Performance Patterns**
   - Debounced storage writes
   - Memory cache for frequent checks
   - TTL-based caching

7. **Monitoring & Debugging**
   - Structured logging (LogLevel enum)
   - Privacy-first telemetry
   - Anonymous metrics only

**Purpose**: Ensure all future extension development follows secure, maintainable patterns.

---

### #51 Rebrand Oneway to Clarity

**Date:** 2026-01-11

**Changes**:
1. **Extension**:
   - `manifest.json`: name → "Clarity - Focus & Flow", short_name → "Clarity"
   - All logs updated: `[Oneway]` → `[Clarity]`
   - Popup UI: "Oneway" → "Clarity"

2. **Desktop App**:
   - Navigation sidebar: Replaced water drop (💧) with diamond (💎) icon
   - Added "Clarity" text next to logo (visible on hover or when pinned)
   - Updated CSS for better logo/text layout

3. **Consistency**:
   - All user-facing text now uses "Clarity"
   - Internal code structure maintained (no breaking changes)

**Rationale**: "Clarity" better represents the core value proposition — achieving mental clarity through focused work.

---

### #52 History Collection Implementation

**Date:** 2026-01-11

**Overview**: Privacy-first browsing history analysis for personalized blocking insights.

**Core Module** (`history-collector.ts`):
- Domain-level categorization (8 categories: social_media, news, video, entertainment, shopping, adult, work, other)
- Automatic URL sanitization (removes query params, fragments)
- Title sanitization (XSS protection, HTML escaping)
- Max 10,000 visits storage with FIFO cleanup
- Real-time monitoring via `webNavigation.onCompleted`

**Permission Flow**:
- Optional `history` permission (not required for basic blocking)
- UX in extension popup:
  - "Enable Insights" card when permission not granted
  - One-click permission request (must be from user gesture)
  - Automatic import of last 30 days after grant
  - Stats display after activation (visits tracked, top distraction)

**Message API**:
- `REQUEST_HISTORY_PERMISSION`: Request permission from popup
- `IMPORT_HISTORY`: Import historical data (configurable days)
- `GET_HISTORY_STATS`: Calculate and return aggregated stats
- `GET_COLLECTION_STATUS`: Check permission and visit count

**Privacy Guarantees**:
- ✅ Only domains stored, no full URLs with query params
- ✅ No content inspection beyond domain
- ✅ All data stored locally (chrome.storage.local)
- ✅ No automatic sync to external servers
- ✅ User can revoke permission anytime
- ✅ Transparent: User sees exactly what's collected

**Security**:
- URL validation (only http/https, no chrome://, no javascript:)
- HTML escaping in titles
- No external tracking
- Input validation on all data

**Documentation**:
- Created `docs/history-collection.md` with:
  - Architecture overview
  - UX flows (Extension first / Desktop first / Settings)
  - Implementation details
  - Privacy & Security practices
  - Testing guidelines
  - Future enhancements roadmap

**Types & Interfaces**:
- `CategorizedVisit`: Sanitized visit with category
- `HistoryStats`: Aggregated statistics (totalVisits, byCategory, topDomains)
- `HistoryCollectionStatus`: Permission state

**Architecture**: Follows `docs/architecture.md` best practices:
- Service worker patterns (no memory state)
- Structured logging
- Type-safe messages
- Input validation & sanitization
- Cleanup automation
- Privacy-first by design

**Next Steps**:
- Native Messaging for Desktop App ↔ Extension communication
- Sync history data to Supabase (user-controlled)
- Dashboard in Desktop App with insights
- Smart recommendations based on patterns
- LLM-based categorization (Phase 3)

---

### #53 Supabase Sync for Navigation History

**Date:** 2026-01-11

**Overview**: User-controlled sync of browsing history to Supabase for cross-device analytics.

**Migration** (`012_navigation_history.sql`):
- `navigation_history` table with user_id, domain, category, visit_time, title, is_distraction
- Row Level Security (RLS) policies
- Indexes for efficient queries

**Architecture** (temporarily disabled):
- Supabase JS client incompatible with Chrome Extension Service Workers
- Service Workers don't have access to `localStorage`, `window` objects
- Planned solution: Direct fetch API calls instead of Supabase client

**Popup UI** (temporarily hidden):
- "Sync to Cloud" section with Sign In / Sync Now buttons
- Will be re-enabled after fetch-based implementation

**Status**: Migration ready, client implementation postponed until fetch refactor.

---

### #54 Native Messaging Implementation

**Date:** 2026-01-11

**Overview**: Real-time bidirectional communication between Chrome Extension and Tauri Desktop App.

**Extension Side** (`native-messaging.ts`):
- `connectToDesktopApp()` — Connect via `chrome.runtime.connectNative`
- `sendToDesktop(message)` — Send typed messages
- `onMessageFromDesktop(handler)` — Register message handlers
- Auto-reconnection (5 attempts, 5s delay)
- Connection status persistence in storage

**Message Protocol**:
- `PING` / `PONG` — Health check
- `GET_AUTH_STATUS` / `AUTH_STATUS` — Sync authentication state
- `GET_CONFIG` / `CONFIG_UPDATE` — Sync blocking rules
- `NAVIGATION_EVENT` — Real-time visit tracking
- `BLOCK_EVENT` — Block/bypass notifications
- `HISTORY_SYNC` — Batch history sync
- `SYNC_REQUEST` — Desktop requests history since timestamp

**Desktop Side (Rust)** (`native_host.rs`):
- Chrome Native Messaging protocol (4-byte length prefix + JSON)
- Message deserialization with serde
- Handler functions for each message type
- Runs via `--native-host` CLI flag

**Native Host Manifest** (`com.clarity.app.json`):
- Installed in `~/Library/Application Support/Google/Chrome/NativeMessagingHosts/`
- Wrapper script to pass `--native-host` flag
- Extension ID whitelisted

**Documentation**: `docs/native-messaging.md` created with architecture details.

**Status**: Infrastructure complete, testing pending (requires Chrome restart).

---

### #55 Browsing Insights Dashboard

**Date:** 2026-01-11

**Overview**: New "Browsing" tab in Stats view for navigation analytics.

**Tab System**:
- `[🌱 Habits] [🔍 Browsing]` toggle in Stats header
- Smooth switching between views
- Header with diamond icon + "Statistics" title

**FocusScoreCard**:
- Large focus score (0-100) with gradient color
- MiniMascot at top (happy/focused/worried based on score)
- Trend indicator (↑ Improving / → Stable / ↓ Declining)
- Subtle glow animation

**TimeDistributionCard**:
- Horizontal stacked bar (Productive/Neutral/Distraction)
- 3 MiniMascots with expressions:
  - Happy (productive)
  - Meh (neutral)
  - Worried (distraction)
- Percentage labels below

**TopSitesCard**:
- Top 5 sites with horizontal bar chart
- Category emoji (✨/🌙/🔥) per site
- Visit count + time spent
- "View all" expands to full list

**BrowsingHeatmapCard**:
- 30-day grid of focus scores
- Color coding: green (>70%), yellow (40-70%), red (<40%)
- Hover tooltips with date + score
- Legend: "Less focused → More focused"

**MiniMascot Component**:
- Compact mascot (28-48px) for inline use
- 4 moods: happy, focused, meh, worried
- Kawaii style with sprout, cheeks, expressive eyes
- Reusable across cards

**Data**:
- Currently using mock data
- Real data will come via Native Messaging from extension
- `useBrowsingStats` hook ready for integration

**UI Polish**:
- Side margins for better centering
- Staggered fade-in animations
- Dark mode compatible (CSS variables)

---

### #56 Complete Data Pipeline

**Date:** 2026-01-11

**Overview**: End-to-end data pipeline from Chrome Extension to Desktop Dashboard.

**Architecture**:
```
Extension → Native Messaging → Rust Storage → Tauri Command → React Hook
```

**Rust Storage Module** (`browsing_data.rs`):
- JSON Lines format (`.jsonl`) for fast appends
- `~/.clarity/clarity-data/visits.jsonl` — navigation history
- `~/.clarity/clarity-data/blocks.jsonl` — block events
- Max 10,000 visits with FIFO cleanup
- Thread-safe via `lazy_static` Mutex

**Data Flow**:
1. Extension sends `NAVIGATION_EVENT` / `BLOCK_EVENT` / `HISTORY_SYNC`
2. Native Host receives and parses JSON
3. `browsing_data::store_*` appends to JSONL files
4. Tauri command `get_browsing_stats` reads and aggregates
5. React hook `useBrowsingStats` invokes command

**Tauri Command**:
```rust
#[tauri::command]
fn get_browsing_stats() -> BrowsingStats
```

**Stats Calculation**:
- Focus Score: `(productive_visits / total_visits) × 100`
- Time Distribution: percentages by category
- Top Sites: sorted by visit count
- Daily Scores: last 30 days
- Trend: compare last 7 days vs previous 7 days

**React Integration**:
- `useBrowsingStats()` hook calls Tauri command
- Auto-refresh every 30 seconds
- Transform Rust types to frontend format

**Dependencies Added**:
- `lazy_static` — thread-safe global storage
- `chrono` — date/time handling
- `dirs` — home directory detection

**Documentation**:
- Created `docs/data-pipeline.md` with full architecture
- Diagrams, data structures, troubleshooting

**Next Steps**:
- Test with Chrome restart
- Verify real data appears in dashboard
- Add real-time updates (WebSocket or Tauri events)

---

### #57 History Import with Period Selection

**Date:** 2026-01-11

**Overview**: Enhanced history import with period selection and smart merge to avoid duplicates.

**Extension Popup Changes**:
- Period selector dropdown: 7 / 30 / 90 / 365 days
- Shown on initial "Enable Insights" card
- "Re-import History" button in insights section
- Progress feedback during import

**Smart Merge Logic** (`history-collector.ts`):
```typescript
// Create signature set for deduplication
const existingSignatures = new Set(
  existingHistory.map(v => `${v.domain}:${v.visitTime}`)
)

// Filter out duplicates
const uniqueNewVisits = newVisits.filter(v => 
  !existingSignatures.has(`${v.domain}:${v.visitTime}`)
)

// Merge and sort
const mergedHistory = [...existingHistory, ...uniqueNewVisits]
  .sort((a, b) => b.visitTime - a.visitTime)
  .slice(0, 10000)
```

**Desktop DataSourceCard**:
- Shows total visits, period start/end, last sync
- Connection status indicator (🔗 Connected / ⚠️ Not synced)
- Refresh button to manually refetch stats

**Desktop Settings**:
- New "Browsing Data" section
- Shows data stats (visits, period)
- Hint to use extension for re-import
- "Clear All Data" button with confirmation

**Rust Backend Updates**:
- Added `periodStart`, `periodEnd`, `lastSync` to `BrowsingStats`
- `clear_browsing_data()` command for settings
- ISO 8601 timestamps for period bounds

**Files Changed**:
- `apps/extension/src/ui/popup/index.html` — period selector UI
- `apps/extension/src/ui/popup/popup.ts` — import logic
- `apps/extension/src/background/history-collector.ts` — smart merge
- `apps/desktop/src/features/stats/components/DataSourceCard.tsx` — new card
- `apps/desktop/src/features/stats/components/BrowsingView.tsx` — integrated card
- `apps/desktop/src/features/stats/hooks/useBrowsingStats.ts` — dataSource meta
- `apps/desktop/src/features/settings/components/SettingsView.tsx` — data section
- `apps/desktop/src-tauri/src/browsing_data.rs` — period + clear
- `apps/desktop/src-tauri/src/lib.rs` — clear command

---

### #58 Data Deduplication & Extension Reset

**Date:** 2026-01-12

**Overview**: Fixed double-sync issues and added user-friendly reset functionality.

**Issue Identified**:
- Desktop app showing ~10,000 visits instead of actual ~5,200
- Root cause: Double-sync from extension (startup sync + manual import)
- Rust backend had no deduplication, appending duplicates

**Deduplication in Rust** (`browsing_data.rs`):
```rust
// Track unique visits by (domain, timestamp)
let mut existing = HashSet::new();
for visit in visits {
  existing.insert((visit.domain.clone(), visit.visit_time));
}

// Only append new unique visits
if !existing.contains(&(visit.domain, visit.visit_time)) {
  // append to file
}
```

**Sync Cooldown** (`service-worker.ts`):
- Added 5-minute cooldown to `syncExistingHistoryToDesktop`
- Prevents redundant syncs on extension startup/reconnect
- Tracks `lastHistorySyncTime` in memory

**Extension Reset Button** (popup UI):
- New "🗑️ Reset Extension Data" button
- Clears `chrome.storage.local` cache
- Confirmation required
- Allows clean re-import after fixing issues

**Data Type Fix**:
- `Math.floor()` applied to all `visitTime` before sending to Rust
- Chrome returns floats, Rust expects `i64` integers
- Fixed "invalid type: floating point" deserialization errors

**Message Batching**:
- History sync now sends data in batches of 500 visits
- 100ms delay between batches
- Avoids 1MB Native Messaging limit
- Progress feedback during sync

**Desktop Settings Improvements**:
- "Clear All Data" button always enabled
- Confirmation alert before deletion
- Improved UX feedback

**Files Changed**:
- `apps/extension/src/ui/popup/index.html` — reset button
- `apps/extension/src/ui/popup/popup.ts` — reset logic
- `apps/extension/src/background/service-worker.ts` — sync cooldown
- `apps/extension/src/background/history-collector.ts` — Math.floor timestamps
- `apps/extension/src/background/native-messaging.ts` — batching
- `apps/desktop/src-tauri/src/browsing_data.rs` — deduplication
- `apps/desktop/src/features/settings/components/SettingsView.tsx` — button fix

**Result**: Data integrity maintained, accurate visit counts, no more duplicates.

---

### #59 Stats UI Polish & Period Selector

**Date:** 2026-01-12

**Overview**: UI improvements for stats dashboard and period filtering foundation.

**Time Distribution Card**:
- Times now displayed under each mascot (aligned properly)
- Removed emojis from contextual messages
- Reduced vertical spacing, added border separator
- Messages: "You're crushing it today!" / "Need a quick reset break?" etc.

**Focus Score Card**:
- Added explanation text: "Based on productive vs distracting sites"
- Clarifies how the score is calculated

**Mini Mascots**:
- `pointer-events: none` → no hover effect on mini mascots
- Only used for visual indicators

**Stats Layout Improvements**:
- Fixed scrolling issue (header was disappearing)
- Header now fixed at top, content scrolls below
- `.stats-view`: flexbox column with `overflow: hidden`
- `.stats-view__tab-content`: scrollable area
- Content max-width: 800px, centered with auto margins
- Better centering with increased side margins

**Period Selector UI** (Option D - Hybrid):
- New `PeriodSelector` component with pills: `Today | 7d | 30d | 90d`
- Pills styled with mint highlight for active state
- Positioned under tabs in header
- Global state `selectedPeriod` in `StatsView`
- Passed as props to `HabitsContent` and `BrowsingView`

**Architecture**:
- Period selector affects ALL cards globally
- Foundation ready for per-card override (Phase 2)
- Backend filtering not yet implemented (shows same data regardless)

**Files Changed**:
- `TimeDistributionCard.tsx` / `.css` — UI polish, alignment
- `FocusScoreCard.tsx` / `.css` — explanation text
- `PeriodSelector.tsx` / `.css` — new component
- `StatsView.tsx` / `.css` — layout fixes, period state
- `BrowsingView.tsx` — period prop
- `useBrowsingStats.ts` — period parameter
- `MiniMascot.css` — pointer-events

**Next Steps**:
- Implement backend filtering (Rust command with period param)
- Per-card period override with 3-dot menu
- Period badge on cards with overrides

---

### #60 Period Filtering Implementation

**Date:** 2026-01-12

**Overview**: Functional period filtering for browsing stats.

**Backend Changes** (`browsing_data.rs`):
- Modified `calculate_stats` to accept `period_days: Option<u32>`
- Filters visits by timestamp before aggregation
- Uses `chrono::Local::now()` minus period days as cutoff
- `None` = all data, `Some(n)` = last n days

**Tauri Command** (`lib.rs`):
- Modified `get_browsing_stats` to accept `period: Option<String>`
- Maps period strings to days:
  - `"today"` → 1 day
  - `"7days"` → 7 days
  - `"30days"` → 30 days
  - `"90days"` → 90 days
  - `"all"` or missing → None (all data)

**Frontend** (`useBrowsingStats.ts`):
- Passes `period` parameter to Tauri invoke
- Re-fetches stats when period changes
- `useEffect` dependency on `[userId, period]`

**Result**:
- Period selector pills now functional
- Stats update immediately when changing period
- Data correctly filtered by timestamp
- All cards respect global period setting

**Next Steps**:
- Per-card period override (3-dot menu)
- Period badge on cards with custom periods
- Cache stats by period for better performance

---

### #61 Per-Card Period Override & Window Size

**Date:** 2026-01-12

**Overview**: Advanced period filtering with per-card overrides and larger default window.

**Per-Card Period Override UI**:
- New `CardPeriodMenu` component with 3-dot trigger
- Appears on hover (top-right corner of cards)
- Dropdown menu with period options + checkmark for active
- "Reset to default" option when card has override
- Period badge displayed on card title when overridden (e.g., "Focus Score (7d)")
- Click outside to close menu

**State Management**:
- `useCardPeriods` hook manages per-card period overrides
- `CardId` type: `focus-score | time-distribution | top-sites | heatmap`
- `getEffectivePeriod(cardId)` returns override or default
- `setCardPeriod(cardId, period)` updates override
- `hasOverride(cardId)` checks if card has custom period

**Integration**:
- Integrated in `FocusScoreCard` and `TimeDistributionCard`
- Props: `period`, `defaultPeriod`, `onPeriodChange`
- Badge appears inline in card title (mint color)
- Menu appears on card hover (opacity 0 → 0.6 → 1 on hover)

**CSS Updates**:
- Cards changed from `overflow: hidden` to `overflow: visible`
- Menu styled with shadows and mint highlights
- Smooth transitions and hover effects

**Window Size**:
- Default: 1600x1000 (previously 1440x900)
- Min size: 1000x800 (previously 900x700)
- Comparable to Slack's default window

**Files Changed**:
- `CardPeriodMenu.tsx` / `.css` — new component
- `useCardPeriods.ts` — new hook
- `FocusScoreCard.tsx` / `.css` — period menu + badge
- `TimeDistributionCard.tsx` / `.css` — period menu + badge
- `BrowsingView.tsx` — hook integration
- `tauri.conf.json` — window size

**Current Limitation**:
- Menu is UI-only, doesn't yet fetch separate data per card
- All cards still use same data source (global period)
- Next: fetch data per card when override is set

**Next Steps**:
- Fetch separate data per card when override active
- Add period menu to TopSitesCard and BrowsingHeatmapCard
- Cache stats by period to avoid redundant fetches

---

### #62 Per-Card Data Fetching & UI Polish

**Date:** 2026-01-12

**Overview**: Complete implementation of per-card data fetching with independent periods.

**UI Improvements**:
- Period menu button **always visible** (opacity 0.5 instead of hidden)
- No hover required to see the button
- Better positioned: `top: var(--space-md); right: var(--space-md)`
- Transparent background by default, fills on hover
- Time Distribution Card: increased spacing below title (`space-md` → `space-lg`)

**Per-Card Data Fetching**:
- New `useBrowsingStatsWithOverride` hook
- **4 parallel fetch calls** via `Promise.all`:
  - Focus Score with its period
  - Time Distribution with its period
  - Top Sites with its period
  - Heatmap with its period
- Each card receives **independent data** based on its override or default period
- Real-time refresh every 30 seconds for all cards
- `useEffect` dependencies on all 4 card periods → refetch when any changes

**Architecture**:
```typescript
cardStats = {
  focusScore: RustBrowsingStats,
  timeDistribution: RustBrowsingStats,
  topSites: RustBrowsingStats,
  heatmap: RustBrowsingStats,
}
```

**Data Mapping**:
- Rust types mapped to frontend types in `BrowsingView`
- Category mapping: `work/dev/productivity` → `productive`
- `SiteVisit` and `DailyFocusScore` interfaces respected

**Performance**:
- Parallel fetches (not sequential) for speed
- 30s refresh interval (configurable)
- No redundant calls (only when period changes)

**Files Changed**:
- `useBrowsingStatsWithOverride.ts` — new hook for parallel fetching
- `BrowsingView.tsx` — integrated new hook, mapped data types
- `CardPeriodMenu.css` — button always visible, better positioning
- `TimeDistributionCard.css` — increased title margin

**Result**: 
- Each card truly independent with its own data period
- UI cleaner with always-visible menu button
- Better spacing and visual hierarchy

**Next Steps**:
- Add period menu to TopSitesCard and BrowsingHeatmapCard
- Optimize with caching (avoid refetch if period unchanged)
- Add loading states per card

---

### #63 Enhanced Period Menu UI & Complete Coverage

**Date:** 2026-01-12

**Overview**: Improved period menu visibility and added to all remaining cards.

**Enhanced Menu UI**:
- **Rectangular button** showing active period (e.g., "30D", "90D", "Today")
- **Dropdown icon** (▼) for clear affordance
- **Better visibility**: always shows what's selected
- More intuitive than the previous "⋮" design
- Positioned at `top: var(--space-sm); right: var(--space-sm)`

**Button Design**:
```
┌──────────┐
│ 30D  ▼  │  ← Active period visible
└──────────┘
```

**Period Labels**:
- `today` → "Today"
- `7days` → "7D"
- `30days` → "30D"
- `90days` → "90D"

**Complete Card Coverage**:
- ✅ FocusScoreCard (already had it)
- ✅ TimeDistributionCard (already had it)
- ✅ **TopSitesCard** (now added)
- ✅ **BrowsingHeatmapCard** (now added)

**Spacing Improvements**:
- TimeDistributionCard: increased spacing below title (`space-lg` → `space-xl`)
- TimeDistributionCard bar: increased bottom margin (`space-lg` → `space-xl`)
- Content sits lower for better visual hierarchy

**CSS Updates**:
- All cards: `position: relative; overflow: visible`
- Menu container: absolute positioning with z-index
- Trigger button: flexible width based on content
- Clean hover states with mint accent

**Integration**:
- All 4 cards now receive `period`, `defaultPeriod`, `onPeriodChange` props
- BrowsingView passes correct props to all cards
- Each card can have independent period selection

**Files Changed**:
- `CardPeriodMenu.tsx` / `.css` — rectangular UI, active period display
- `TimeDistributionCard.css` — increased spacing
- `TopSitesCard.tsx` / `.css` — added menu
- `BrowsingHeatmapCard.tsx` / `.css` — added menu
- `BrowsingView.tsx` — integrated all menus

**Result**:
- Period selection now intuitive and visible on all cards
- User can see at a glance which period each card is using
- Complete feature parity across all browsing insight cards

**Next Steps**:
- Add loading skeleton per card during fetch
- Cache mechanism to avoid redundant API calls
- Optimize parallel fetches with request deduplication

---

### #64 Stats Cards Final Polish

**Date:** 2026-01-12

**Overview**: Final UI polish for stats cards based on user feedback.

**1. TimeDistributionCard - Fixed Mascots**:
- Disabled floating animation on mini mascots
- Added `animation: none !important` to `.mini-mascot__blob` within legend
- Mascots now static (no more bounce effect)
- Cleaner, less distracting UI

**2. BrowsingHeatmapCard - Removed Redundant Text**:
- Removed "Last 30 days" text from header
- Period now visible in the dropdown menu instead
- Less clutter, cleaner header

**3. TopSitesCard - Dropdown Selector**:
- Replaced "View all (10)" button with dropdown menu
- **Options**: Top 10 / Top 20 / Top 30
- Same visual style as period menu (rectangular, with ▼ icon)
- Positioned at left of header (replaced old toggle button)
- Dropdown shows:
  - Header: "Display"
  - Options with checkmark for active selection
  - Hover and active states

**Implementation Details**:

TopSitesCard dropdown:
```typescript
type DisplayLimit = 10 | 20 | 30
const [displayLimit, setDisplayLimit] = useState<DisplayLimit>(10)
const displayedSites = sites.slice(0, displayLimit)
```

**CSS Style**:
- `.top-sites-card__limit-trigger` — button matching period menu style
- `.top-sites-card__limit-dropdown` — dropdown menu
- Checkmark (✓) on active option
- Click outside to close

**Files Changed**:
- `TimeDistributionCard.css` — disabled mascot animation
- `BrowsingHeatmapCard.tsx` — removed period text
- `TopSitesCard.tsx` — dropdown implementation
- `TopSitesCard.css` — dropdown styles

**Result**:
- Cleaner, more polished stats cards
- Consistent interaction patterns across all cards
- Better UX with visible selection states
- Less visual noise

**Next Steps**:
- Add smooth transitions when changing display limits
- Persist user preferences (period + display limit)
- Add keyboard navigation for dropdowns

---

### #65 Global Period Reset & Text Visibility

**Date:** 2026-01-13

**Overview**: Improved text visibility and global period reset behavior.

**1. Global Period Reset**:
- Clicking ANY global period button resets ALL individual card overrides
- Even clicking the same period value triggers a reset
- New `resetCounter` in StatsView increments on every click
- New `resetAllOverrides()` function in `useCardPeriods` hook
- BrowsingView listens to `resetTrigger` prop via `useEffect`

**Flow**:
```
User sets Global = 7D
User overrides FocusScore to 30D
User clicks 7D again (or any other period)
→ ALL overrides reset, all cards use global period
```

**2. Improved Text Visibility** (light/dark mode):

| Element | Before | After |
|---------|--------|-------|
| "Based on productive..." | 11px, text-muted | **12px, text-secondary** |
| Legend labels | 11px, text-muted | **12px, text-secondary, font-weight 500** |
| Percentages | 16px | **18px, font-weight 700** |
| Neutral value | text-secondary | **text-primary** |
| Legend time | 11px, text-muted | **12px, text-secondary** |
| Message | 13px, text-secondary | **14px, text-primary, font-weight 500** |

**3. UI Cleanups**:
- Category filter pills with colored dots
- Scrollable TopSitesCard list (max-height 520px)
- Tab icons: SVG sun (habits) + globe (browsing)
- Reduced margins (max-width 1000px)

**Files Changed**:
- `useCardPeriods.ts` — `resetAllOverrides()` function
- `StatsView.tsx` — `resetCounter`, `handleGlobalPeriodChange`
- `BrowsingView.tsx` — `resetTrigger` prop, useEffect listener
- `FocusScoreCard.css` — explanation text visibility
- `TimeDistributionCard.css` — legend + message visibility
- `TopSitesCard.tsx/css` — category dots, filter pills, scrollable list
- `StatsView.css` — tab icon sizing, margin adjustments

---

### #66 Site Classification Modal

**Date:** 2026-01-13

Ajout d'une modal compacte pour classifier manuellement les sites (productif/neutre/distraction).

**Fonctionnalités**:
1. **Table compacte** avec sites triés par volume de visites
2. **Icônes cute** (pas d'emojis):
   - `★` Productive (mint)
   - `◇` Neutral (gris)
   - `✕` Distraction (coral)
3. **Progress bar** montrant le % de navigation classifiée
4. **Toggle behavior** : re-cliquer désélectionne
5. **Deux points d'accès**:
   - TopSitesCard → bouton "Improve classification"
   - Settings → section "Browsing Data" → "Manage Site Classification"

**UI**:
```
┌───────────────────────────────────────────────────────┐
│ Site Classification                            [×]    │
├───────────────────────────────────────────────────────┤
│ Classify your top sites to improve focus accuracy     │
│ ● 45% of browsing classified                          │
│ ████████████░░░░░░░░░░░░░░░░                          │
├───────────────────────────────────────────────────────┤
│ Site              Visits    ★     ◇     ✕            │
│ ──────────────────────────────────────────────────── │
│ twitter.com         89      ○     ○     ●            │
│ github.com          67      ●     ○     ○            │
│ notion.so           45      ○     ●     ○            │
├───────────────────────────────────────────────────────┤
│                                  [Cancel]    [Save]   │
└───────────────────────────────────────────────────────┘
```

**Files Created**:
- `SiteClassificationModal.tsx` — Modal component
- `SiteClassificationModal.css` — Styles

**Files Modified**:
- `TopSitesCard.tsx` — "Improve classification" button + modal integration
- `TopSitesCard.css` — Button styles
- `SettingsView.tsx` — "Manage Site Classification" button + modal
- `SettingsView.css` — Secondary button styles
- `BrowsingView.tsx` — Classification save handler

**Next Steps**:
- Persister les classifications dans le backend Rust
- Appliquer les classifications à la catégorisation des sites
- Sync vers Supabase (user preferences)

---

### #67 Habits Calendar View + Timezone Fix + Inline Reclassify

**Date:** 2026-01-14

Major improvements to habits UI and browsing stats.

**Habits — Calendar View** 📅:
- New Google Calendar-style view with hour grid (6h-22h)
- Habits positioned by `scheduled_time`, height by `duration_minutes`
- NOW marker positioned at current time
- Boundaries displayed as hatched background zones
- Click to toggle check/uncheck
- 3 view modes: Calendar 📅 | Timeline ▤ | List ☰

**Habits — UI Improvements**:
- Timeline view now default (was list)
- Cards more compact but still readable
- Boundaries section separated at bottom with active badge
- "+ Add Habit" button more visible with dashed border

**Timezone Fix** 🕐:
- Fixed habits not resetting at midnight local time
- Was using `toISOString()` (UTC) instead of local date
- Applied to: `habits.ts`, `useTodayCheckIns.ts`, `calculations.ts`, `BrowsingHeatmapCard.tsx`

**Browsing Stats — Inline Reclassify**:
- Click on category dot in TopSitesCard → dropdown to reclassify
- "reclassify" hint appears on hover
- Saves immediately to backend

**Browsing Stats — Other Fixes**:
- Filter out GPS coordinates from domain list (`is_valid_domain` function)
- Classification modal fetches its own unfiltered data (all sites, all time)
- Silent background refresh (no UI flicker, no modal closing)
- Fix mapCategory to handle user classifications ('productive', 'distraction')

**Files Modified**:
- `HabitList.tsx` — Calendar view + view toggle + boundaries section
- `HabitList.css` — Calendar grid styles + compact cards
- `habits.ts` — `getLocalToday()` function for timezone fix
- `useTodayCheckIns.ts` — Local date for optimistic check-ins
- `calculations.ts` — Local date for streak/stats calculations
- `BrowsingHeatmapCard.tsx` — Local date for heatmap
- `TopSitesCard.tsx` — Inline reclassify dropdown
- `TopSitesCard.css` — Reclassify dropdown styles
- `SiteClassificationModal.tsx` — Fetch own data, blend auto+manual classifications
- `App.css` — Add habit button more visible
- `browsing_data.rs` — `is_valid_domain()` filter + remove truncate(50)
- `BrowsingView.tsx` — Add mapCategory cases for user values

**Calendar View UI**:
```
┌──────────────────────────────────────────┐
│ 06:00 ─────────────────────────────────  │
│ 07:00 ┌─ ✨ Méditation ───────────── ✓ ┐ │
│       └────────────────────────────────┘ │
│ 08:00 ─────────────────────────────────  │
│ 09:00 ┌─ 💻 Deep work ─────────────────┐ │
│ 10:00 │                                │ │
│       └────────────────────────────────┘ │
│ 11:00 ── ● NOW 11:34 ──────────────────  │
│ 12:00 ─────────────────────────────────  │
└──────────────────────────────────────────┘
```

**Next Steps**:
- Drag & drop to create/move habits in calendar view
- Auto-detect boundary violations from browsing history

---

### #68 Calendar Drag & Drop with 15-min Snap

**Date:** 2026-01-14

Implemented drag & drop for calendar view with Google Calendar-style snapping.

**15-Minute Snap Grid**:
- Visual gridlines at 15-minute intervals (subtle)
- `snapY()` function rounds Y position to nearest 15px (15 min)
- Magnetic feel: elements "jump" between grid lines during drag

**Drag to Create**:
- Click and drag on empty space
- Preview rectangle shows selected time range
- Release creates new habit with calculated duration

**Drag to Move**:
- Drag existing habit blocks
- Snaps to 15-minute positions while dragging
- Updates `scheduled_time` on release

**Scroll Fix**:
- Calendar now properly scrollable (hours column syncs with grid)
- Fixed structure: hours column + scrollable grid container

**View Switch Fix**:
- Added `silent` parameter to `refetchHabits()`
- Drag operations use silent refetch to avoid unmounting HabitList
- ViewMode state now persists during habit create/update

**Files Modified**:
- `HabitList.tsx` — Snap logic, scroll sync, drag handlers
- `HabitList.css` — Quarter-hour gridlines, scroll container
- `useHabits.ts` — Silent refetch option
- `App.tsx` — Silent refetch for drag operations

**Next Steps**:
- Auto-detect boundary violations from browsing history
- Edit habit directly after drag-create

---

### #69 Modal UI Polish + Delete from Edit

**Date:** 2026-01-15

Major UI improvements to habit modals and calendar view.

**Modal Styling** (aligned with GoalModal):
- Overlay: `backdrop-filter: blur(4px)`, opacity 0.4
- Background: `--bg-elevated` instead of `--bg-primary`
- Border-radius: 16px
- Spacing: padding 24px 28px, gap 24px

**Form Layout**:
- Name field first, Icon picker to the right
- Icon picker compact: 42x42 box, no "Change" text
- Time interval: Start (time input) + Duration (dropdown)
- Toggle Do/Avoid: cleaner style with `--bg-card` background

**Inputs Styling**:
- Background: `--bg-card`
- Border: `--border-default`
- Focus: mint border + green glow (`box-shadow`)

**Calendar Checkboxes**:
- Smaller (16x16 in calendar, 20x20 in timeline)
- Mint tint background instead of white
- Thinner border (1.5px)

**Delete from Edit Modal**:
- Delete button added to EditHabitModal
- Red color, positioned left of Cancel/Save
- Direct delete (no confirm dialog - Tauri issue)

**Other Fixes**:
- "Today" period filter uses midnight local time (not 24h ago)
- Calendar drag snaps to floor (earlier time slot)
- View mode persisted in localStorage
- Calendar icons: SVG instead of emoji

**Files Modified**:
- `AddHabitModal.tsx` / `.css`
- `EditHabitModal.tsx` / `.css`
- `EmojiPicker.tsx` / `.css`
- `HabitList.tsx` / `.css`
- `App.tsx`
- `browsing_data.rs` / `lib.rs` (timezone fix)

---

### #70 Blocking System: SafeSearch + Search Filter + Protection Status

**Date:** 2026-01-17

Implemented comprehensive blocking features to protect users from explicit content.

**1. Google SafeSearch Enforcement** (`rules.json`):
- `declarativeNetRequest` rules automatically append `&safe=active` to Google searches
- Covers 10 Google domains (google.com, google.fr, google.de, google.co.uk, etc.)
- Network-level enforcement — happens before page loads

**2. Search Query Detection & Blocking** (`search-filter.ts`):
- Keyword list + regex patterns for explicit content detection
- Intercepts searches on Google, Bing, DuckDuckGo, Yahoo
- Blocks and redirects to block screen BEFORE results load
- Tracks blocked searches count per day
- Integrated in `webNavigation.onBeforeNavigate` (before regular blocking rules)

**3. Protection Status API**:
- Extension sends `PROTECTION_STATUS` message to desktop on connect
- Rust backend stores status in global state (`ExtensionStatus`)
- Tauri command `get_extension_status` exposes status to frontend
- React hook `useExtensionStatus` polls status every 10 seconds

**4. BoundariesView Protection Section**:
- New section at top of Boundaries view showing 4 status items:
  - Extension connection status (✅ Connected / ⚠️ Not connected)
  - Incognito protection (✅ Protected / ⚠️ Not enabled + Setup button)
  - SafeSearch (✅ Enforced)
  - Search Filter (✅ Active + blocked count)
- "Setup" button opens incognito setup modal

**5. Incognito Setup Modal** (`IncognitoSetupModal.tsx`):
- Step-by-step guide with numbered steps
- Instructions to enable extension in incognito mode
- Clear explanation of why it matters

**6. Extension Popup Warning**:
- Orange warning banner if incognito not enabled
- Shows at top of popup, above status section
- "Setup" button with instructions

**Architecture**:
```
Extension (search-filter.ts)
    ↓ webNavigation.onBeforeNavigate
    ↓ extractSearchQuery() + isExplicitSearch()
    ↓ Redirect to block screen OR continue
    
Extension (native-messaging.ts)
    ↓ sendProtectionStatusToDesktop()
    ↓ PROTECTION_STATUS message
    
Desktop (native_host.rs)
    ↓ handle_message() → update_protection_status()
    ↓ ExtensionStatus global state
    
Desktop (lib.rs)
    ↓ get_extension_status() Tauri command
    
React (useExtensionStatus.ts)
    ↓ invoke('get_extension_status')
    ↓ BoundariesView displays status
```

**Files Created**:
- `apps/extension/src/background/search-filter.ts`
- `apps/desktop/src/features/boundaries/hooks/useExtensionStatus.ts`
- `apps/desktop/src/features/boundaries/components/IncognitoSetupModal.tsx`
- `apps/desktop/src/features/boundaries/components/IncognitoSetupModal.css`

**Files Modified**:
- `apps/extension/public/rules.json` — SafeSearch rules
- `apps/extension/src/background/service-worker.ts` — Search filter integration
- `apps/extension/src/background/native-messaging.ts` — Protection status message
- `apps/extension/src/shared/types.ts` — ProtectionStatus interface
- `apps/extension/src/ui/popup/index.html` — Incognito warning
- `apps/extension/src/ui/popup/popup.ts` — Incognito check
- `apps/desktop/src-tauri/src/native_host.rs` — ExtensionStatus storage
- `apps/desktop/src-tauri/src/lib.rs` — get_extension_status command
- `apps/desktop/src-tauri/Cargo.toml` — once_cell dependency
- `apps/desktop/src/features/boundaries/components/BoundariesView.tsx` — Protection section
- `apps/desktop/src/features/boundaries/components/BoundariesView.css` — Protection styles

**Documentation**: Created `docs/blocking.md` with full architecture.

**Next Steps**:
- Test search blocking with explicit queries
- Add AI-based query classification (Phase 2)

---

### #71 SafeSearch Expansion — All Major Search Engines

**Date:** 2026-01-17

Expanded SafeSearch enforcement to cover all major search engines worldwide.

**New Search Engines Covered**:

| Engine | Parameter | Notes |
|--------|-----------|-------|
| **Google** | `safe=active` | 20 TLDs (.com, .fr, .de, .co.uk, .es, .it, .ca, .com.au, .ch, .be, .nl, .pt, .com.br, .com.mx, .co.in, .co.jp, .co.kr, .ru, .pl, .at) |
| **Bing** | `adlt=strict` | search, images, videos |
| **DuckDuckGo** | `kp=1` | search |
| **Yahoo** | `vm=r` | search, images, FR subdomain |
| **Ecosia** | `safeSearch=strict` | search |
| **Qwant** | `safesearch=2` | search |
| **Brave** | `safesearch=strict` | search, images, videos |
| **Yandex** | `family=yes` | .com, .ru, images |

**Total: 37 declarativeNetRequest rules**

**Search Filter Updates** (`search-filter.ts`):
- Added support for all new engines in `extractSearchQuery()`
- Added support for all new engines in `isSearchEngine()`
- Query parameter mapping:
  - Yandex uses `text=` instead of `q=`
  - StartPage uses `query=`

**Documentation Updates**:

`docs/feature-ideas.md`:
- Added "Safeguards & Protection avancée" section:
  - DNS-level blocking
  - Health check & alertes
  - Alerte incognito persistante
  - Panic mode
  - Tamper detection
  - Multi-navigateur

`docs/blocking.md`:
- Updated "Domaines couverts" with complete list
- Added detailed future phases:
  - Phase 2: Safeguards & Resilience (DNS, health check, alerts)
  - Phase 3: Modes de Strictness (Panic mode)
  - Phase 4: Intelligence
  - Phase 5: Multi-platform
  - Phase 6: Reporting

**Files Modified**:
- `apps/extension/public/rules.json` — 37 rules (was 10)
- `apps/extension/src/background/search-filter.ts` — All engines support
- `docs/feature-ideas.md` — Safeguards section
- `docs/blocking.md` — Future phases documentation

---

### #72 Heartbeat System & Protection Alerts (Phase 2)

**Date:** 2026-01-17

Implemented Phase 2 of the blocking safeguards: real-time health monitoring with heartbeat system and visual alerts.

**Architecture Overview**:

```
Extension                              Desktop
    │                                      │
    ├── HEARTBEAT (60s interval) ─────────►│
    │   {timestamp, status, version}       │
    │                                      ├── Store in ~/.clarity/extension-status.json
    │                                      │
    │                                      ├── Compute alertLevel:
    │                                      │   < 90s  = "ok"
    │                                      │   90s-5m = "warning"
    │                                      │   > 5m   = "critical"
    │                                      │
    │                                      ├── UI polls every 5s
    │                                      │   └── ProtectionAlert banner
```

**Extension Side** (`native-messaging.ts`):
- `startHeartbeat()` — Starts 60-second interval
- `stopHeartbeat()` — Stops on disconnect
- `sendHeartbeat()` — Sends full protection status + timestamp + version
- Heartbeat starts automatically on connection
- Stops automatically on disconnect

**Rust Backend** (`native_host.rs`):
- New `HeartbeatData` struct with all protection fields
- New `AlertLevel` enum: `Ok`, `Warning`, `Critical`
- `update_heartbeat()` — Stores heartbeat data
- `compute_alert_level()` — Dynamically computes based on elapsed time
- `get_extension_status()` — Returns status with computed alert level

**Constants**:
```rust
ALERT_WARNING_THRESHOLD_MS = 90_000   // 90 seconds
ALERT_CRITICAL_THRESHOLD_MS = 300_000 // 5 minutes
```

**Frontend** (`useExtensionStatus.ts`):
- New fields: `lastHeartbeat`, `heartbeatCount`, `extensionVersion`, `alertLevel`
- Polls every 5 seconds (was 10s) for faster detection
- Removes manual "connected" calculation (now done in Rust)

**ProtectionAlert Component**:
- Visual banner for warning (orange) and critical (red) states
- Critical has pulse animation for urgency
- Shows time since last heartbeat
- "Check Extension" button with instructions
- Warning can be dismissed, critical cannot

**Global Alert in App**:
- Shows at top of all views (except Boundaries, which has its own)
- Fixed position below titlebar
- Respects sidebar width

**UI Updates in BoundariesView**:
- Protection Status section now shows alert level with icon
- Shows heartbeat timestamp when OK
- Different styling for ok/warning/critical states
- Shows "8 search engines" detail for SafeSearch

**Files Created**:
- `apps/desktop/src/features/boundaries/components/ProtectionAlert.tsx`
- `apps/desktop/src/features/boundaries/components/ProtectionAlert.css`

**Files Modified**:
- `apps/extension/src/background/native-messaging.ts` — Heartbeat system
- `apps/desktop/src-tauri/src/native_host.rs` — AlertLevel, heartbeat handling
- `apps/desktop/src/features/boundaries/hooks/useExtensionStatus.ts` — New fields
- `apps/desktop/src/features/boundaries/components/BoundariesView.tsx` — Alert integration
- `apps/desktop/src/features/boundaries/components/BoundariesView.css` — Alert level styles
- `apps/desktop/src/features/boundaries/index.ts` — Exports
- `apps/desktop/src/app/App.tsx` — Global ProtectionAlert
- `apps/desktop/src/app/App.css` — Alert positioning
- `docs/blocking.md` — Updated Phase 2 as implemented

**Next Steps**:
- System notifications (macOS/Windows) for critical alerts
- Tray icon badge when protection compromised
- DNS-level blocking as backup
- Phase 3: Panic mode (optional)

---

### #73 Aoi Floating Widget — Browser Companion

**Date:** 2026-01-17

Implemented Aoi as a floating widget companion in the browser, visible on every webpage.

**Features**:

| Feature | Description |
|---------|-------------|
| **Floating Bubble** | 56px circular widget fixed in bottom-right corner |
| **Aoi Character** | Kawaii mascot with sprout, cheeks, and legs |
| **3 Status Levels** | OK (green), Nudge (orange), Alert (red) |
| **Hover Messages** | Context-aware messages on hover |
| **Time Tracking** | Shows time spent on current page |
| **Shadow DOM** | Isolated styles, unaffected by page CSS |

**Status Levels**:

| Status | Color | Aoi Mood | Trigger |
|--------|-------|----------|---------|
| `ok` | 🟢 Mint | 😊 Happy | Protection active, productive site |
| `nudge` | 🟠 Orange | 🤔 Concerned | 10+ min on distraction site |
| `alert` | 🔴 Red | 😟 Worried | Protection issue, blocked site |

**Messages**:
- OK: "Tout roule ✓", "Tu es focus 💪", "Protection active ✓"
- Nudge: "Tu es sur Twitter depuis 15 min..."
- Alert: "⚠️ Protection inactive !"

**Technical Implementation**:

```
Content Script (content-script.ts)
    │
    ├── createAoiWidget()
    │   ├── Shadow DOM (isolated styles)
    │   ├── CSS animations (float, wiggle, pulse)
    │   └── Event listeners
    │
    ├── GET_AOI_STATUS → Service Worker
    │   └── Returns: alertLevel, isDistraction, siteCategory
    │
    └── Update loop (30s interval)
```

**Distraction Detection**:
- Social: twitter.com, facebook.com, instagram.com, tiktok.com, reddit.com
- Video: youtube.com, netflix.com, twitch.tv
- News: hackernews.com, buzzfeed.com
- Gaming: discord.com

**Excluded URLs** (no widget):
- chrome://
- chrome-extension://
- about:
- file://

**CSS Features**:
- Status ring with colored border
- Pulse animations for nudge/alert
- Float animation for Aoi body
- Leg wiggle animation
- Hover scale effect
- Message bubble with arrow

**Files Created**:
- `apps/extension/src/content/content-script.ts` — Full Aoi widget implementation

**Files Modified**:
- `apps/extension/src/background/service-worker.ts` — GET_AOI_STATUS handler
- `apps/extension/vite.config.ts` — Content script build
- `apps/extension/manifest.json` — Content script injection config

**Usage**:
1. Reload extension in chrome://extensions
2. Visit any webpage
3. Aoi appears in bottom-right corner
4. Hover to see message, click to minimize

**Next Steps**:
- Click to open mini-panel with stats
- More sophisticated time tracking per site
- Integration with desktop app for unified status
- Custom messages based on user goals

---

### #74 Aoi Widget Options Menu — Hide Global & Per Domain

**Date:** 2026-01-18

Added options menu to Aoi widget with two hide options: global and per-domain.

**Features**:

| Feature | Description |
|---------|-------------|
| **Options Menu** | Click on Aoi → popup menu with actions |
| **Hide everywhere** | 🌐 Hides Aoi on ALL websites |
| **Hide on this site** | 📍 Hides Aoi only on current domain |
| **Restore Button** | Mini Aoi button appears when hidden, click to restore |
| **Smart Restore** | Restores correct setting based on how it was hidden |

**Menu UI**:
```
┌───────────────────┐
│ 🌐 Hide everywhere │
│ 📍 Hide on this site │
└───────────────────┘
        ▼
      [Aoi]
```

**Storage**:
- `clarity_hidden_global` — boolean for global hide
- `clarity_hidden_domains` — array of hidden domains

**Logic**:
1. Check global hide first (priority)
2. Then check domain-specific hide
3. On restore, undo the correct setting (global or domain)

**Restore Button**:
- Small 28px circular button with mini Aoi icon
- Appears in bottom-right corner when Aoi is hidden
- 40% opacity, full opacity on hover
- Click to show Aoi again

**Files Modified**:
- `apps/extension/src/content/content-script.ts` — Options menu with 2 hide options

**Usage**:
1. Click on Aoi → menu appears
2. Click "Hide everywhere" → Aoi hidden on all sites
3. OR click "Hide on this site" → Aoi hidden only here
4. Click restore button → Aoi returns

**Next Steps**:
- Add more menu options (e.g., "Open Clarity", "Take a break")
- Settings page to manage hidden domains list
- Keyboard shortcut to toggle visibility

---

### #75 Aoi Preferences Persistence — Supabase Sync

**Date:** 2026-01-18

Full persistence pipeline for Aoi widget visibility preferences — synced to Supabase.

**Architecture**:
```
Extension (chrome.storage.local)
    ↓ AOI_PREFERENCES_UPDATE
Service Worker
    ↓ Native Messaging
Desktop Native Host (Rust)
    ↓ ~/.clarity/aoi-preferences.json
Desktop Frontend (React)
    ↓ Polls file every 5s
Supabase (user_settings table)
```

**Supabase Migration** (`014_aoi_preferences.sql`):
```sql
ALTER TABLE user_settings
ADD COLUMN aoi_hidden_global BOOLEAN DEFAULT false;
ADD COLUMN aoi_hidden_domains TEXT[] DEFAULT '{}';
```

**Extension Changes**:
- New message types: `AOI_PREFERENCES_UPDATE`, `GET_AOI_PREFERENCES`
- Content script calls `syncPreferencesToDesktop()` on hide/show
- Service worker forwards to native messaging
- On connect, extension requests prefs via `GET_AOI_PREFERENCES`

**Desktop Rust Changes** (`native_host.rs`):
- `AoiPreferencesData` struct
- `get_aoi_preferences()` / `save_aoi_preferences()` — local file storage
- Handlers for `AOI_PREFERENCES_UPDATE` and `GET_AOI_PREFERENCES`

**Desktop Tauri Commands** (`lib.rs`):
- `get_aoi_preferences` — read from local file
- `save_aoi_preferences` — write to local file

**Desktop Frontend** (`useAoiPreferences.ts`):
- Hook that syncs between Supabase and local file
- On mount: fetch from Supabase, save to local file
- Polls local file every 5s for extension updates
- Auto-syncs changes to Supabase

**API Functions** (`settings.ts`):
- `getAoiPreferences(userId)` — fetch from Supabase
- `updateAoiPreferences(userId, prefs)` — save to Supabase

**Data Flow**:
1. User hides Aoi in extension → chrome.storage.local
2. Extension → Service Worker → Native Messaging → Rust
3. Rust saves to `~/.clarity/aoi-preferences.json`
4. Desktop polls file, detects change, syncs to Supabase
5. On next extension connect, prefs restored from Supabase

**Files Created**:
- `supabase/migrations/014_aoi_preferences.sql`
- `apps/desktop/src/features/settings/hooks/useAoiPreferences.ts`

**Files Modified**:
- `apps/extension/src/shared/types.ts` — AoiPreferences interface
- `apps/extension/src/background/native-messaging.ts` — new message types
- `apps/extension/src/background/service-worker.ts` — AOI_PREFERENCES_UPDATE handler
- `apps/extension/src/content/content-script.ts` — syncPreferencesToDesktop()
- `apps/desktop/src-tauri/src/native_host.rs` — Aoi prefs handling
- `apps/desktop/src-tauri/src/lib.rs` — Tauri commands
- `apps/desktop/src/features/onboarding/api/settings.ts` — Supabase API

**Benefits**:
- ✅ Preferences persist across browser reinstalls
- ✅ Preferences sync across devices (via Supabase)
- ✅ Works offline (local file as cache)
- ✅ Extension gets prefs on connect (fast startup)

---

### #76 Intelligent Blocking — Phase 1 & 2 Implementation

**Date:** 2026-01-18

Implemented the first two phases of the intelligent blocking system for advanced content filtering.

**Phase 1 — Foundations (Types & Keywords)**:

| Component | Description |
|-----------|-------------|
| **Types** (`shared/types.ts`) | `SearchSession`, `SearchEntry`, `SearchAnalysisResult`, `SearchFlag`, `HeightenedModeState`, `SearchThresholds` |
| **Explicit Keywords** (`keywords/explicit.ts`) | 100+ explicit terms with 3 severity levels (instant block: 100, high: 50, medium: 25) |
| **Suspicious Combinations** (`keywords/suspicious.ts`) | Modifiers + Subjects + Suffixes system (e.g., "hot" + "girl" + "video" = 40 points) |
| **Multi-language** (`keywords/multilang.ts`) | 11 languages: JP, CN, ES, DE, PT, FR, RU, IT, AR, NL, KR (~200 terms) |
| **Anti-Evasion** (`lib/normalizer.ts`) | Character substitution (p0rn→porn), spacing (p o r n→porn), Cyrillic lookalikes, typo correction |

**Phase 2 — Search Intelligence Engine**:

| Feature | Implementation |
|---------|----------------|
| **Multi-layer Analysis** | Keywords → Combinations → Multilang → Evasion → Behavioral |
| **Session Tracking** | 5-minute rolling window, accumulates scores across searches |
| **Behavioral Detection** | Frantic pattern (5+ searches/60s), Escalation pattern |
| **Scoring System** | 0-100 scale, thresholds: warn (20), block (50), heightened (100) |
| **Heightened Mode** | Auto-activates for 30min, lowers all thresholds |
| **Daily Stats** | Tracks blocks, warnings, heightened activations |

**Architecture**:
```
User Search Query
       │
       ▼
┌──────────────────────────┐
│ 1. Normalize (anti-evasion) │
│    p0rn → porn             │
└──────────────────────────┘
       │
       ▼
┌──────────────────────────┐
│ 2. Check Explicit Keywords │
│    +50 to +100 points      │
└──────────────────────────┘
       │
       ▼
┌──────────────────────────┐
│ 3. Check Combinations     │
│    +25 to +40 points       │
└──────────────────────────┘
       │
       ▼
┌──────────────────────────┐
│ 4. Check Multi-language   │
│    +50 points              │
└──────────────────────────┘
       │
       ▼
┌──────────────────────────┐
│ 5. Behavioral Analysis    │
│    +15 to +20 points       │
└──────────────────────────┘
       │
       ▼
┌──────────────────────────┐
│ 6. Determine Action       │
│    < 20: allow            │
│    20-50: warn            │
│    > 50: block            │
└──────────────────────────┘
```

**Files Created**:
- `apps/extension/src/background/search-intelligence.ts` — Main scoring engine
- `apps/extension/src/shared/keywords/explicit.ts` — Explicit keyword lists
- `apps/extension/src/shared/keywords/suspicious.ts` — Combination detection
- `apps/extension/src/shared/keywords/multilang.ts` — Multi-language support
- `apps/extension/src/shared/keywords/index.ts` — Barrel exports
- `apps/extension/src/lib/normalizer.ts` — Anti-evasion normalization

**Files Modified**:
- `apps/extension/src/shared/types.ts` — New intelligent blocking types
- `apps/extension/src/background/service-worker.ts` — Integration with new engine
- `docs/blocking/implementation.md` — Technical documentation

**Documentation**:
- `docs/blocking/intelligent-blocking.md` — Architecture overview
- `docs/blocking/implementation.md` — Implementation guide

**Testing Needed**:
```
// Test explicit keyword detection
Search: "pornhub" → BLOCK (score: 100)
Search: "xxx video" → BLOCK (score: 100)

// Test suspicious combinations
Search: "hot girl video" → WARN/BLOCK (score: 40)
Search: "sauna xxx" → BLOCK (score: 25 + 100 = 125)

// Test anti-evasion
Search: "p0rn" → BLOCK (normalized to "porn")
Search: "pr0n" → BLOCK (typo corrected)
Search: "рorn" (cyrillic р) → BLOCK (normalized)

// Test multi-language
Search: "エロ動画" → BLOCK (Japanese explicit)
Search: "色情" → BLOCK (Chinese explicit)

// Test behavioral
5+ searches in 60s → +20 points (frantic)
Escalating scores → +15 points (escalation)

// Test heightened mode
Total session score > 100 → Mode activates
Thresholds lowered for 30 minutes
```

**Next Steps**:
- Phase 3: Heightened Mode UI + notifications
- Phase 4: Content Analysis (Layer 3 - page content)
- Phase 5: Hard blocklist expansion (~500 domains)

---

### #77 Anti-Evasion Improvements — Repeated Characters & Japanese

**Date:** 2026-01-18

Renforcé la détection anti-évasion après tests utilisateur.

**Problèmes identifiés**:
1. `pooooorno` n'était pas bloqué (caractères répétés)
2. `えろろろ` (japonais avec répétition) passait à travers
3. Hiragana manquant (seulement katakana était couvert)

**Corrections apportées**:

**1. Normalizer — Répétition aggressive**:
```
Avant: (.)\1{2,} → $1$1  (3+ → 2)
Après: (.)\1+   → $1     (2+ → 1)

pooooorno → porno (avant: poorno ❌)
sexxxy    → sexy
```

**2. Multilang — Hiragana + Répétition**:
```typescript
// Ajout hiragana
'えろ', 'せっくす', 'えっち'  // + katakana existant

// Normalisation répétée pour tous scripts
えろろろ → えろ ✓
порнооо → порно ✓
```

**3. Typos étendus**:
```typescript
'porno' → 'porn'
'porrrn' → 'porn'
'sexx' → 'sex'
'boobss' → 'boobs'
```

**Files Modified**:
- `apps/extension/src/lib/normalizer.ts` — Regex `(.)\1+`, typos étendus
- `apps/extension/src/shared/keywords/multilang.ts` — Hiragana, `normalizeRepeatedChars()`
- `docs/blocking/implementation.md` — Documentation mise à jour

**Tests à faire**:
| Query | Expected | Reason |
|-------|----------|--------|
| `pooooorno` | BLOCK | Repeated chars → porno → porn |
| `p0rn` | BLOCK | 0→o substitution |
| `えろろろ` | BLOCK | JP repeated → えろ |
| `エロ` | BLOCK | Katakana |
| `えろ` | BLOCK | Hiragana (new) |
| `sexxxy` | BLOCK | Repeated → sexy |
| `breast cancer` | ALLOW | Safe context |
